// Package render builds the PR comment markdown (PLAN.md §5.5). Pure
// string-in, string-out — tested with golden files. Status-grouped sort,
// collapsed no-ops, replacement warnings, per-stack gate trace.
package render
